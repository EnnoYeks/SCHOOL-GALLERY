(function () {
    if (window.__hshsUpload) return;
    window.__hshsUpload = true;

    // NOTE: full file content is large - loading from local artifact via next approach
    console.error('upload file incomplete');
})();
